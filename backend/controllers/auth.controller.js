export const registerUser = async (req, res) => {
  res.status(200).json({ message: "Register user endpoint placeholder" });
};

export const loginUser = async (req, res) => {
  res.status(200).json({ message: "Login user endpoint placeholder" });
};
