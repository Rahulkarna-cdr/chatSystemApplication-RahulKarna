export const sendMessage = async (req, res) => {
  res.status(200).json({ message: "Send message endpoint placeholder" });
};

export const getMessages = async (req, res) => {
  res.status(200).json({ message: "Get messages endpoint placeholder" });
};
