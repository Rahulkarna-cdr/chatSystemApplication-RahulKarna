export const createChat = async (req, res) => {
  res.status(200).json({ message: "Create chat endpoint placeholder" });
};

export const getChats = async (req, res) => {
  res.status(200).json({ message: "Get chats endpoint placeholder" });
};
